#include <cstdio>
#include <algorithm>
#include <cstirng>
using namespace std;

int main() {
	bool flag = 0;
	int t, n, l, b;
	scanf("%d", &t);
	while(t--) {
		scanf("%d", &n);
		while(n--) {
			scanf("%d%d", &l, &b);
			switch(b%3) {
				case '0':
					
			}
		}
	}


	return 0;
}